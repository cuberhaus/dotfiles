#!/bin/bash

if laptop-detect ; then
    setxkbmap es 
fi
betterlockscreen -u ~/wallpapers/landscape
xmodmap -e "clear lock"
xmodmap -e "keycode 9 = Caps_Lock NoSymbol Caps_Lock"
xmodmap -e "keycode 66 = Escape NoSymbol Escape"
